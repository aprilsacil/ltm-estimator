# LifeTrackMed Technical Exam

## Installation
1. clone the repository `https://github.com/aprilsacil/ltm-estimator.git`
2. run `composer install` inside the project folder
3. run `yarn build` inside the project folder